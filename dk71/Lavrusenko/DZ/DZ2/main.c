#include "area.h"



int main(){
	
	Point *points = 4;

	points = (Point*) malloc(4*sizeof(Point));

	printf("program for calculating squared area\n");
	
    check_enter(points);
	
printf("area = %f",length_sqere(Side));
	
free_all(points);
	
	
	
	
}
