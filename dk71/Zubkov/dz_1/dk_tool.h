 typedef struct rever_t //обьявление структуры rever_t и типа данных rever_t
    {
    int size;	
    int *array; // переменные обьекта
    } rever_t;
 rever_t ArrRever(rever_t arr_info); //обьявляем функцию с типом данных rever_t (для возвращения обьекта rever_t после выполнения функции)
 
